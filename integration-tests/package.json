{
  "name": "slender-integration-tests",
  "version": "1.0.0",
  "description": "",
  "scripts": {
    "test-develop": "yarn install && cross-env NODE_ENV=develop ./deploy.sh && cross-env NODE_ENV=develop node_modules/mocha/bin/_mocha --bail --timeout 999999 --colors",
    "test-futurenet": "yarn install && cross-env NODE_ENV=futurenet ./deploy.sh && cross-env NODE_ENV=futurenet node_modules/mocha/bin/_mocha --bail --timeout 999999 --colors"
  },
  "author": "EQ LAB",
  "license": "ISC",
  "devDependencies": {
    "@types/chai": "^4.3.5",
    "@types/mocha": "^10.0.1",
    "@types/node": "^20.4.1",
    "bigint-conversion": "^2.4.1",
    "bignumber.js": "^9.1.1",
    "buffer": "^6.0.3",
    "chai": "^4.3.7",
    "cross-env": "^7.0.3",
    "dotenv": "^16.3.1",
    "mocha": "^10.2.0",
    "soroban-client": "0.9.1",
    "ts-node": "^10.9.1",
    "typescript": "^5.1.6"
  }
}